# bridge-and-split

Bridge 0.01 USDC from Solana Devnet to Base Sepolia. Then split the token using Locker Split Plugin.

```bash
bun install
```

To run:

```bash
cp .env.example .env
bun run index.ts
```
